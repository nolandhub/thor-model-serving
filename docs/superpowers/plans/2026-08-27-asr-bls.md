# Tách encoder thành model riêng có batch (BLS) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tách encoder của `asr_streaming` thành một model Triton riêng có dynamic batching và implicit state, để nhiều stream dùng chung một lần gọi encoder — nâng CCU bền vững vượt mức 4 hiện tại.

**Architecture:** Ba model cùng tồn tại: `asr_streaming` (đường cũ, giữ nguyên làm đối chứng), `encoder` (ONNX, `sequence_batching` + implicit state), `asr_streaming_bls` (python: fbank + greedy, gọi `encoder` qua BLS). State encoder do Triton giữ, không bao giờ đi qua python.

**Tech Stack:** Triton Inference Server, ONNX Runtime (CUDA EP), Python backend, numpy, pytest.

**Spec:** `docs/superpowers/specs/2026-08-27-asr-bls-design.md`

## Global Constraints

- Thor không ra được internet: **không `pip install`, không `docker pull`**. Chỉ dùng package đã có trong image.
- Mọi file code mở đầu bằng hai dòng `# ABOUTME:`.
- Triton chạy `--model-control-mode=none`: **sửa `config.pbtxt` phải `docker compose restart asr`** thì mới có hiệu lực.
- `model_repository/asr_streaming/` là đường đối chứng — **không sửa một dòng nào**.
- Test chạy được khi Thor tắt: phần thuần tách khỏi phần chạm mạng/GPU.
- Bench chạy trong network compose (`./scripts/serving.sh bench`), không chạy trên host.
- p99 < 200ms và drift không dồn là ràng buộc cứng của mọi kết quả nghiệm thu.

---

## File Structure

| File | Trách nhiệm |
|---|---|
| `scripts/gen_encoder_config.py` | Đọc `encoder.onnx`, sinh `config.pbtxt` cho model `encoder`. Phần dựng chuỗi là hàm thuần |
| `tests/test_gen_encoder_config.py` | Test hàm thuần của trên |
| `model_repository/encoder/config.pbtxt` | Sinh ra, commit vào repo |
| `model_repository/encoder/1/model.onnx` | Symlink/copy từ `asr_streaming/1/encoder.onnx` |
| `model_repository/asr_streaming_bls/1/model.py` | Python backend: fbank + greedy, gọi `encoder` qua BLS |
| `model_repository/asr_streaming_bls/config.pbtxt` | Như `asr_streaming` nhưng đổi tên |
| `bench/run_asr.py` | Thêm `--model` để bench trỏ được sang đường BLS |

---

### Task 1: Script sinh config cho model `encoder`

74 khối `state` không thể gõ tay — sai một tên là Triton từ chối nạp model với thông báo không chỉ ra chỗ sai. Sinh từ chính file ONNX.

**Files:**
- Create: `scripts/gen_encoder_config.py`
- Test: `tests/test_gen_encoder_config.py`

**Interfaces:**
- Consumes: `bench.encoder_batch.batch_axis(shape, symbol)` — đã có, trả index của chiều batch theo tên ký hiệu động.
- Produces:
  - `state_pairs(input_names, output_names) -> list[tuple[str, str]]`
  - `triton_dims(shape, batch_ax) -> list[int]`
  - `render_config(name, x_name, x_dims, x_type, states, max_batch, max_candidate) -> str`
    trong đó `states` là `list[dict]` với khoá `input_name`, `output_name`, `data_type`, `dims`.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_gen_encoder_config.py
# ABOUTME: Test phần dựng config.pbtxt cho model encoder - hàm thuần, không cần file ONNX
# ABOUTME: Chạy: pytest tests/test_gen_encoder_config.py

import sys
from pathlib import Path

import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

from scripts.gen_encoder_config import render_config, state_pairs, triton_dims  # noqa: E402


# --- state_pairs: ghép state vào/ra THEO VỊ TRÍ ------------------------------
# Đây đúng bất biến mà model.py hiện tại đang dựa vào và đã khẳng định bằng
# assert: input[1:] khớp output[1:] theo thứ tự. Ghép theo tên sẽ hỏng vì tên
# vào và tên ra khác nhau (cached_x vs new_cached_x).


def test_state_pairs_ghep_theo_vi_tri():
    assert state_pairs(["a_in", "b_in"], ["a_out", "b_out"]) == [
        ("a_in", "a_out"), ("b_in", "b_out")
    ]


def test_state_pairs_lech_so_luong_bao_loi():
    with pytest.raises(ValueError, match="lệch"):
        state_pairs(["a_in"], ["a_out", "b_out"])


# --- triton_dims: bỏ chiều batch --------------------------------------------
# max_batch_size > 0 thì Triton NGẦM hiểu chiều đầu là batch và dims khai phần
# còn lại. Khai cả chiều batch vào dims là model nạp được nhưng shape sai.


def test_triton_dims_bo_chieu_batch():
    assert triton_dims(["N", 2, 512], 0) == [2, 512]


def test_triton_dims_batch_khong_o_axis_0_bao_loi():
    # Triton chỉ batch được khi chiều batch đứng ĐẦU. State có batch ở giữa thì
    # không dùng được max_batch_size, phải biết ngay chứ không phải sau khi nạp
    with pytest.raises(ValueError, match="axis 0"):
        triton_dims([2, "N", 512], 1)


def test_triton_dims_chieu_dong_khac_batch_bao_loi():
    with pytest.raises(ValueError, match="động"):
        triton_dims(["N", "T", 512], 0)


# --- render_config -----------------------------------------------------------


def _cfg():
    return render_config(
        name="encoder",
        x_name="x",
        x_dims=[45, 80],
        x_type="TYPE_FP32",
        states=[{"input_name": "c_in", "output_name": "c_out",
                 "data_type": "TYPE_FP32", "dims": [2, 512]}],
        max_batch=8,
        max_candidate=8,
    )


def test_render_config_co_ten_va_backend():
    cfg = _cfg()
    assert 'name: "encoder"' in cfg
    assert 'platform: "onnxruntime_onnx"' in cfg


def test_render_config_khai_state_du_ba_phan():
    cfg = _cfg()
    assert 'input_name: "c_in"' in cfg
    assert 'output_name: "c_out"' in cfg
    assert "dims: [ 2, 512 ]" in cfg


def test_render_config_initial_state_zero():
    # Đúng như _Stream.__init__ của đường cũ: state khởi tạo bằng zeros
    assert "zero_data: true" in _cfg()


def test_render_config_dung_oldest_de_gom_nhieu_sequence():
    # oldest là chiến lược DUY NHẤT gom được request từ nhiều sequence khác nhau
    # vào một batch - direct thì mỗi sequence một slot riêng, avg_batch mãi 1.0
    assert "oldest" in _cfg()
    assert "max_candidate_sequences: 8" in _cfg()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_gen_encoder_config.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'scripts.gen_encoder_config'`

- [ ] **Step 3: Write minimal implementation**

```python
# scripts/gen_encoder_config.py
# ABOUTME: Sinh config.pbtxt cho model encoder từ encoder.onnx - 74 khối state không gõ tay được
# ABOUTME: Chạy: python scripts/gen_encoder_config.py > model_repository/encoder/config.pbtxt

import argparse
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))

from bench.encoder_batch import batch_axis  # noqa: E402

_ORT_TO_TRITON = {
    "tensor(float)": "TYPE_FP32",
    "tensor(float16)": "TYPE_FP16",
    "tensor(int64)": "TYPE_INT64",
    "tensor(int32)": "TYPE_INT32",
}


def state_pairs(input_names, output_names):
    """Ghép state vào/ra THEO VỊ TRÍ, đúng bất biến mà model.py đang dựa vào.

    Không ghép theo tên: tên vào và tên ra khác nhau (cached_x vs new_cached_x),
    và quy ước đặt tên đổi theo bản export. Vị trí thì không đổi - model.py hiện
    tại đã khẳng định điều đó bằng assert.
    """
    if len(input_names) != len(output_names):
        raise ValueError(
            f"số state vào ({len(input_names)}) lệch số state ra ({len(output_names)})"
        )
    return list(zip(input_names, output_names))


def triton_dims(shape, batch_ax):
    """Shape của ONNX -> dims của Triton: bỏ chiều batch, cấm mọi chiều động khác.

    max_batch_size > 0 thì Triton NGẦM hiểu chiều đầu là batch, dims chỉ khai
    phần còn lại. Và chiều batch bắt buộc đứng đầu - state có batch ở giữa thì
    không dùng được max_batch_size, phải hỏng ngay ở đây chứ không phải sau khi
    Triton nạp model rồi trả shape sai lúc chạy.
    """
    if batch_ax != 0:
        raise ValueError(
            f"chiều batch nằm ở axis {batch_ax}, Triton chỉ batch được khi nó ở axis 0"
        )
    dims = []
    for dim in shape[1:]:
        if isinstance(dim, str):
            raise ValueError(f"shape {shape} còn chiều động {dim!r} ngoài batch")
        dims.append(dim)
    return dims


def render_config(name, x_name, x_dims, x_type, states, max_batch, max_candidate):
    """Dựng nội dung config.pbtxt. Thuần chuỗi, không đọc file nào."""
    blocks = []
    for s in states:
        dims = ", ".join(str(d) for d in s["dims"])
        blocks.append(
            f"""    {{
      input_name: "{s['input_name']}"
      output_name: "{s['output_name']}"
      data_type: {s['data_type']}
      dims: [ {dims} ]
      initial_state: {{
        data_type: {s['data_type']}
        dims: [ {dims} ]
        zero_data: true
        name: "zero"
      }}
    }}"""
        )
    x_dims_str = ", ".join(str(d) for d in x_dims)
    states_str = ",\n".join(blocks)
    return f"""# SINH TỰ ĐỘNG bởi scripts/gen_encoder_config.py - đừng sửa tay.
# 74 khối state ghép theo VỊ TRÍ giữa input[1:] và output[1:] của encoder.onnx.

name: "{name}"
platform: "onnxruntime_onnx"
max_batch_size: {max_batch}

input [
  {{
    name: "{x_name}"
    data_type: {x_type}
    dims: [ {x_dims_str} ]
  }}
]

output [
  {{
    name: "encoder_out"
    data_type: {x_type}
    dims: [ -1, -1 ]
  }}
]

sequence_batching {{
  # oldest là chiến lược DUY NHẤT gom được request từ nhiều sequence khác nhau
  # vào cùng một batch. direct gán mỗi sequence một slot cố định, avg_batch sẽ
  # mãi là 1.0 và toàn bộ lý do tồn tại của model này biến mất.
  oldest {{
    max_candidate_sequences: {max_candidate}
  }}
  state [
{states_str}
  ]
}}

instance_group [
  {{
    kind: KIND_GPU
    count: 1
  }}
]
"""


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--onnx", default=str(ROOT / "model_repository/asr_streaming/1/encoder.onnx"))
    ap.add_argument("--name", default="encoder")
    ap.add_argument("--max-batch", type=int, default=8)
    ap.add_argument("--max-candidate", type=int, default=8)
    args = ap.parse_args()

    import onnxruntime as ort

    sess = ort.InferenceSession(args.onnx, providers=["CPUExecutionProvider"])
    inputs, outputs = sess.get_inputs(), sess.get_outputs()
    x = inputs[0]
    symbol = x.shape[0]
    if not isinstance(symbol, str):
        raise SystemExit(f"chiều batch của {x.name} cố định ({symbol}) - model này không batch được")

    pairs = state_pairs([i.name for i in inputs[1:]], [o.name for o in outputs[1:]])
    states = []
    for (in_name, out_name), spec in zip(pairs, inputs[1:]):
        states.append({
            "input_name": in_name,
            "output_name": out_name,
            "data_type": _ORT_TO_TRITON[spec.type],
            "dims": triton_dims(spec.shape, batch_axis(spec.shape, symbol)),
        })

    print(render_config(
        name=args.name,
        x_name=x.name,
        x_dims=triton_dims(x.shape, batch_axis(x.shape, symbol)),
        x_type=_ORT_TO_TRITON[x.type],
        states=states,
        max_batch=args.max_batch,
        max_candidate=args.max_candidate,
    ), end="")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_gen_encoder_config.py -q && ruff check scripts/ tests/`
Expected: PASS, ruff sạch

- [ ] **Step 5: Commit**

```bash
git add scripts/gen_encoder_config.py tests/test_gen_encoder_config.py
git commit -m "bls: script sinh config.pbtxt cho model encoder từ file ONNX"
```

---

### Task 2: Spike 0 — implicit state có chạy trên Thor không (CỔNG CHẶN)

**Không viết `asr_streaming_bls` trước khi task này xanh.** Implicit state với ORT backend chưa từng chạy trên Thor. Sản phẩm của task này là **một câu trả lời**, script probe là đồ bỏ.

**Files:**
- Create: `model_repository/encoder/config.pbtxt` (sinh ra), `model_repository/encoder/1/model.onnx`
- Create (throwaway): `/tmp/.../probe_encoder.py`

**Interfaces:**
- Consumes: `scripts/gen_encoder_config.py` từ Task 1.
- Produces: câu trả lời đi/không đi + giá trị `decode_chunk_len` ghi vào `bench/README.md`.

- [ ] **Step 1: Sinh config và dựng thư mục model**

```bash
cd /u01/nhandlt2/thor-model-serving
mkdir -p model_repository/encoder/1
cp model_repository/asr_streaming/1/encoder.onnx model_repository/encoder/1/model.onnx
docker compose --profile asr --profile bench run --rm --entrypoint python3 bench \
  scripts/gen_encoder_config.py > model_repository/encoder/config.pbtxt
head -40 model_repository/encoder/config.pbtxt
```

Expected: config có `name: "encoder"`, `state [` và 74 khối.

**Nếu bước này ném `chiều batch nằm ở axis N`:** dừng. State có batch không ở axis 0 nghĩa là model không dùng được `max_batch_size` của Triton. Ghi lại axis thật của từng state rồi quay lại thiết kế — đường lùi là truyền state tay (spec §3).

- [ ] **Step 2: Đọc `decode_chunk_len`**

```bash
docker compose --profile asr --profile bench run --rm --entrypoint python3 bench -c \
  "import onnxruntime as ort; s=ort.InferenceSession('model_repository/asr_streaming/1/encoder.onnx', providers=['CPUExecutionProvider']); print(s.get_modelmeta().custom_metadata_map)"
```

Ghi lại `decode_chunk_len`. Nó quyết định tần suất gọi encoder: mỗi chunk 200ms nạp 20 khung, mỗi bước encoder tiêu thụ `decode_chunk_len` khung. `decode_chunk_len = 32` nghĩa là mỗi stream chỉ gọi encoder ~320ms một lần — con số này vào thẳng phần chọn `max_queue_delay` ở Task 5.

- [ ] **Step 3: Nạp model và xác nhận Triton chịu**

```bash
docker compose restart asr
sleep 25
curl -s localhost:4000/v2/models/encoder/config | python3 -m json.tool | head -30
./scripts/serving.sh logs asr | grep -iE "encoder|state|error" | tail -20
```

Expected: `curl` trả config có `sequence_batching`, log không có lỗi nạp model.

**Nếu Triton từ chối nạp:** đọc thông báo lỗi trong log, ghi lại nguyên văn. Đây là kết quả spike — báo cáo rồi dừng, đừng vá lấy được.

- [ ] **Step 4: Probe — state có được giữ đúng giữa các lần gọi không**

```python
# probe_encoder.py (ĐỒ BỎ - không commit)
# So encoder chạy qua Triton (implicit state) với chạy thẳng bằng onnxruntime.
# Nếu Triton không giữ state, lần gọi thứ hai trở đi sẽ lệch.
import numpy as np
import onnxruntime as ort
import tritonclient.grpc as grpcclient

ONNX = "model_repository/asr_streaming/1/encoder.onnx"
sess = ort.InferenceSession(ONNX, providers=["CPUExecutionProvider"])
x_spec = sess.get_inputs()[0]
T, F = x_spec.shape[1], x_spec.shape[2]

# tham chiếu: chạy thẳng, tự giữ state
states = [
    np.zeros([1 if isinstance(d, str) else d for d in i.shape], dtype=np.float32)
    for i in sess.get_inputs()[1:]
]
names = [i.name for i in sess.get_inputs()[1:]]
ref = []
rng = np.random.default_rng(0)
chunks = [rng.standard_normal((1, T, F)).astype(np.float32) for _ in range(3)]
for x in chunks:
    outs = sess.run(None, {x_spec.name: x, **dict(zip(names, states))})
    states = outs[1:]
    ref.append(outs[0])

# qua Triton: chỉ gửi x, state do Triton giữ
client = grpcclient.InferenceServerClient("localhost:4001")
got = []
for i, x in enumerate(chunks):
    inp = grpcclient.InferInput("x", list(x.shape), "FP32")
    inp.set_data_from_numpy(x)
    res = client.infer(
        "encoder", [inp], sequence_id=12345,
        sequence_start=(i == 0), sequence_end=(i == len(chunks) - 1),
    )
    got.append(res.as_numpy("encoder_out"))

for i, (a, b) in enumerate(zip(ref, got)):
    diff = np.abs(a - b).max()
    print(f"chunk {i}: lệch tối đa {diff:.2e} {'OK' if diff < 1e-3 else 'SAI'}")
```

```bash
docker compose --profile asr --profile bench run --rm --entrypoint python3 bench probe_encoder.py
```

Expected: cả ba chunk `OK`.

**Chunk 0 khớp mà chunk 1, 2 lệch = Triton KHÔNG giữ state.** Đó là kết quả spike, không phải bug cần vá. Báo cáo và lùi về truyền tay.

- [ ] **Step 5: Commit phần giữ lại**

```bash
git add model_repository/encoder/
git commit -m "bls: model encoder với implicit state, config sinh từ encoder.onnx"
```

---

### Task 3: Model `asr_streaming_bls`

**Files:**
- Create: `model_repository/asr_streaming_bls/config.pbtxt`
- Create: `model_repository/asr_streaming_bls/1/model.py`
- Create: `model_repository/asr_streaming_bls/1/streaming_search.py` (copy)

**Interfaces:**
- Consumes: model `encoder` từ Task 2.
- Produces: model Triton tên `asr_streaming_bls`, cùng input/output với `asr_streaming` (`AUDIO_CHUNK` → `TRANSCRIPT`).

- [ ] **Step 1: Sao đường cũ nguyên vẹn**

```bash
mkdir -p model_repository/asr_streaming_bls/1
cp model_repository/asr_streaming/1/model.py model_repository/asr_streaming_bls/1/
cp model_repository/asr_streaming/1/streaming_search.py model_repository/asr_streaming_bls/1/
cp model_repository/asr_streaming/1/bpe.model model_repository/asr_streaming_bls/1/
cp model_repository/asr_streaming/1/decoder.onnx model_repository/asr_streaming_bls/1/
cp model_repository/asr_streaming/1/joiner.onnx model_repository/asr_streaming_bls/1/
sed 's/name: "asr_streaming"/name: "asr_streaming_bls"/' \
  model_repository/asr_streaming/config.pbtxt > model_repository/asr_streaming_bls/config.pbtxt
```

Không copy `encoder.onnx`: model này không nạp encoder nữa.

- [ ] **Step 2: Bỏ session encoder trong `initialize`**

Trong `model_repository/asr_streaming_bls/1/model.py`, xoá dòng

```python
        self.encoder = ort.InferenceSession(os.path.join(d, "encoder.onnx"), providers=providers)
```

và thay bằng đọc metadata một lần rồi bỏ session ngay - model này không chạy
encoder nữa, giữ session là tốn thêm một CUDA context cho mỗi instance (bench
đo được context chiếm phần lớn 529 MiB mỗi instance):

```python
        # Chỉ cần metadata và shape của encoder, không cần chạy nó -> mở bằng
        # CPU provider rồi bỏ ngay, không giữ session (không tốn CUDA context).
        enc_path = os.path.join(
            args["model_repository"], "..", "asr_streaming", "1", "encoder.onnx"
        )
        probe = ort.InferenceSession(enc_path, providers=["CPUExecutionProvider"])
        meta = probe.get_modelmeta().custom_metadata_map
        self.decode_chunk_len = int(meta["decode_chunk_len"])
        enc_inputs = probe.get_inputs()
        self.x_name = enc_inputs[0].name
        self.x_dtype = _ORT_TO_NP[enc_inputs[0].type]
        self.T = int(enc_inputs[0].shape[1])
        del probe
```

Xoá `self.encoder = ort.InferenceSession(...)`, xoá `self.state_in_names`, `self.state_out_count`, `self.init_state_specs`.

- [ ] **Step 3: `_Stream` không giữ state nữa**

```python
class _Stream:
    """State của một stream đang sống. Cache encoder KHÔNG nằm ở đây nữa -
    Triton giữ nó theo correlation_id, xem config của model encoder."""

    def __init__(self, model):
        self.fbank = StreamingFbank()
        self.feat = np.zeros((0, NUM_MEL_BINS), dtype=np.float32)
        self.search = init_search_state(model.run_decoder, BLANK_ID, CONTEXT_SIZE)
        self.last_seen = time.monotonic()
        self.started = False   # đã gửi SEQUENCE_START xuống encoder chưa
```

- [ ] **Step 4: `_encoder_step` gọi BLS**

```python
    def _encoder_step(self, stream, corrid, feat_chunk, end=False):
        """Một bước encoder qua BLS. State do Triton giữ theo corrid.

        Cờ START phải gửi ở lần gọi ĐẦU TIÊN của stream, END ở lần cuối - quên
        END thì slot state trong Triton không bao giờ được giải phóng, và số
        sequence đồng thời sẽ chạm max_candidate_sequences rồi treo stream mới.
        """
        x = pb_utils.Tensor(self.x_name, feat_chunk[None].astype(self.x_dtype))
        flags = 0
        if not stream.started:
            flags |= pb_utils.TRITONSERVER_REQUEST_FLAG_SEQUENCE_START
            stream.started = True
        if end:
            flags |= pb_utils.TRITONSERVER_REQUEST_FLAG_SEQUENCE_END
        req = pb_utils.InferenceRequest(
            model_name="encoder",
            requested_output_names=["encoder_out"],
            inputs=[x],
            correlation_id=corrid,
            flags=flags,
        )
        resp = req.exec()
        if resp.has_error():
            raise RuntimeError(f"encoder lỗi: {resp.error().message()}")
        out = pb_utils.get_output_tensor_by_name(resp, "encoder_out").as_numpy()
        return out[0].astype(np.float32)
```

`_advance()` nhận thêm `corrid` và truyền xuống; `_handle()` truyền `corrid` nó đã đọc.

- [ ] **Step 5: `_sweep` phải gửi END cho stream mồ côi**

```python
    def _sweep(self):
        """Xoá state của stream chết không gửi END.

        Khác đường cũ: ngoài dict trong python, còn phải báo Triton giải phóng
        slot state của encoder. Bỏ qua bước đó thì python sạch mà Triton vẫn
        giữ cache vĩnh viễn - rò rỉ chỉ lộ ra khi chạm max_candidate_sequences.
        """
        now = time.monotonic()
        for k in [k for k, s in self.streams.items() if now - s.last_seen > STATE_TTL_S]:
            pb_utils.Logger.log_warn(f"asr_streaming_bls: xoá state mồ côi corrid={k}")
            if self.streams[k].started:
                try:
                    self._encoder_step(
                        self.streams[k], k,
                        np.full((self.T, NUM_MEL_BINS), LOG_EPS, dtype=np.float32),
                        end=True,
                    )
                except Exception as e:   # noqa: BLE001
                    pb_utils.Logger.log_warn(f"asr_streaming_bls: không đóng được corrid={k}: {e}")
            del self.streams[k]
```

- [ ] **Step 6: Nạp và kiểm parity**

```bash
docker compose restart asr
sleep 25
curl -s localhost:4000/v2/models/asr_streaming_bls/config | python3 -m json.tool | head -5
ASR_MODEL=asr_streaming_bls ./tests/test_parity.sh
```

Expected: transcript đúng bằng golden `ANH CÓ THÍCH TÔI GIỮ TRƯỚC QUYỂN SÁCH NÀY CHO ANH KHÔNG`.

`test_parity.sh` chưa nhận biến môi trường chọn model. Trong `tests/test_parity.sh`,
ngay sau khối đọc `.env` (chỗ đặt `GRPC_PORT` và `ADDR`), thêm:
```bash
MODEL="${ASR_MODEL:-asr_streaming}"
```
rồi trong lệnh gọi client, thêm `--model "$MODEL"` vào mảng tham số truyền cho
`client/asr_streaming_client.py` (đứng cạnh `--url`).

`client/asr_streaming_client.py` đang hardcode tên model ở dòng gọi
`client.async_stream_infer("asr_streaming", [inp], ...)`. Sửa hai chỗ:

1. Thêm vào khối `argparse` (cạnh `--url`):
```python
    ap.add_argument("--model", default="asr_streaming")
```

2. Đổi chuỗi hardcode thành biến:
```python
        client.async_stream_infer(
            args.model,
            [inp],
            sequence_id=seq_id,
            sequence_start=(i == 0),
            sequence_end=(i == len(parts) - 1),
        )
```

- [ ] **Step 7: Commit**

```bash
git add model_repository/asr_streaming_bls/ tests/test_parity.sh client/asr_streaming_client.py
git commit -m "bls: model asr_streaming_bls gọi encoder qua BLS, state do Triton giữ"
```

---

### Task 4: Bench trỏ được sang đường BLS

**Files:**
- Modify: `bench/run_asr.py` (hằng `MODEL` ở đầu file, `main()`)

**Interfaces:**
- Consumes: model `asr_streaming_bls` từ Task 3.
- Produces: `--model` trên `bench/run_asr.py`, mặc định `asr_streaming`.

- [ ] **Step 1: Thay hằng `MODEL` bằng tham số**

Trong `bench/run_asr.py`: xoá `MODEL = "asr_streaming"`, thêm

```python
    ap.add_argument("--model", default="asr_streaming", help="asr_streaming | asr_streaming_bls")
```

rồi truyền `args.model` vào `run_once()`, `warmup()`, `_run_stream()` và `read_counters(...)` thay cho `MODEL`. `--config-url` cũng phải theo model — đổi mặc định trong `compose.yaml` thành `http://asr:8000/v2/models` rồi ghép tên model vào trong `main()`:

```python
    cfg_url = f"{args.config_url.rstrip('/')}/{args.model}/config" if args.config_url else None
```

- [ ] **Step 2: Chạy lại đường cũ để chứng minh không đổi gì**

```bash
./scripts/serving.sh bench --ccu 4 --runs 1
```

Expected: p99 và queue nằm trong khoảng của mốc chuẩn (`bench/README.md`: p99 ~0.189s, queue ~141µs ở CCU 4). Lệch nhiều nghĩa là đã làm hỏng driver, không phải BLS nhanh hơn.

- [ ] **Step 3: Commit**

```bash
git add bench/run_asr.py compose.yaml
git commit -m "bench: thêm --model để đo được cả đường BLS"
```

---

### Task 5: Quét `max_queue_delay` và chốt kết quả

**Files:**
- Modify: `model_repository/encoder/config.pbtxt` (mỗi lần quét)
- Modify: `bench/README.md` (ghi kết quả)
- Modify: `scripts/gen_encoder_config.py` (thêm `--max-queue-delay`)

**Interfaces:**
- Consumes: mọi thứ ở trên.
- Produces: giá trị `max_queue_delay_microseconds` chốt lại, và kết luận đi/không đi ghi trong `bench/README.md`.

- [ ] **Step 1: Thêm `--max-queue-delay` vào script sinh config**

Trong `render_config()`, thêm tham số `max_queue_delay=0` (**phải có giá trị mặc
định**, nếu không mọi test đã viết ở Task 1 gọi thiếu tham số sẽ gãy) và chèn vào
khối `oldest`:

```python
  oldest {{
    max_candidate_sequences: {max_candidate}
    max_queue_delay_microseconds: {max_queue_delay}
  }}
```

Thêm test tương ứng vào `tests/test_gen_encoder_config.py`:

```python
def test_render_config_khai_max_queue_delay():
    cfg = render_config(
        name="encoder", x_name="x", x_dims=[45, 80], x_type="TYPE_FP32",
        states=[{"input_name": "c_in", "output_name": "c_out",
                 "data_type": "TYPE_FP32", "dims": [2, 512]}],
        max_batch=8, max_candidate=8, max_queue_delay=10000,
    )
    assert "max_queue_delay_microseconds: 10000" in cfg
```

- [ ] **Step 2: Quét, mỗi giá trị một lần đo**

Với mỗi giá trị trong `{0, 5000, 10000, 20000, 40000}`:

```bash
docker compose --profile asr --profile bench run --rm --entrypoint python3 bench \
  scripts/gen_encoder_config.py --max-queue-delay 10000 > model_repository/encoder/config.pbtxt
docker compose restart asr && sleep 25
curl -s localhost:4000/v2/models/encoder/config | grep -i queue_delay   # XÁC NHẬN đã nạp
./scripts/serving.sh bench --model asr_streaming_bls
```

**Bắt buộc `restart` và bắt buộc `curl` xác nhận.** Triton nạp config một lần lúc khởi động; bỏ bước này thì mọi bảng sẽ giống nhau và mất cả buổi mới nhận ra (đã xảy ra ngày 2026-08-27).

- [ ] **Step 3: Đối chiếu với ràng buộc cứng**

Với mỗi giá trị, ghi lại: CCU cao nhất còn thoả **p99 < 200ms VÀ drift không dồn**, kèm `avg_batch` và `bls_tax`.

- CCU bền vững > 4 → BLS thắng, chốt giá trị `max_queue_delay` cho CCU cao nhất
- `bls_tax` > 0.3 → thuế ăn phần lớn phần thắng, dừng lại xem xét
- Mọi giá trị đều không vượt 4 → **BLS không mua được gì ở quy mô này**. Đây là kết quả hợp lệ, không phải thất bại

- [ ] **Step 4: Ghi kết quả vào `bench/README.md`**

Thêm mục "Kết quả BLS" sau mục mốc chuẩn: bảng CCU của đường BLS, giá trị `max_queue_delay` đã chọn, `decode_chunk_len` đọc được ở Task 2, và kết luận một dòng. Nếu kết luận là không đi tiếp thì ghi rõ vì sao, để lần sau không ai đi lại đúng con đường này.

- [ ] **Step 5: Commit**

```bash
git add model_repository/encoder/config.pbtxt scripts/gen_encoder_config.py tests/test_gen_encoder_config.py bench/README.md
git commit -m "bls: chốt max_queue_delay và ghi kết quả đo vào bench/README"
```
