# Trọng số model - Thor không ra được internet, rsync từ máy dev sang (xem README)

*.bin

# Cấu hình cục bộ của từng máy - commit .env.example thay vì file này
.env

scripts/hello-v2.json


# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

.claude/

# Báo cáo bench - sinh lại được từ bench/run_asr.py, số phụ thuộc máy đo
bench/results/
bench/result/
.gitnexus
CLAUDE.local.md

# Fixture bench - nặng vài MB tới vài chục MB và không sinh lại được từ repo,
# nên vừa không nên nằm trong git vừa không được xoá nhầm. hello-v2.json đã
# được liệt kê sẵn ở trên (dòng 8); giữ nguyên, thêm luật chung cho các bộ sau.
scripts/hello-v2.json
scripts/candidates_top128.json
